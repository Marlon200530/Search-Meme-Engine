{
  "compilerOptions": {
    "target": "ES6",                    // Define o alvo para ES6
    "module": "commonjs",               // Módulo CommonJS (compatível com Node.js)
    "strict": true,                     // Habilita as verificações de tipo estritas
    "esModuleInterop": true,            // Habilita a interoperabilidade entre módulos
    "skipLibCheck": true,               // Ignora a verificação de tipos em bibliotecas
    "outDir": "../dist",                // Pasta onde os arquivos compilados serão gerados (um nível acima de 'src')
    "rootDir": ".",                     // Define o diretório raiz como o próprio 'src' (onde o tsconfig está)
    "sourceMap": true                   // Gera arquivos de mapa de origem para depuração
  },
  "include": [
    "**/*.ts"                           // Inclui todos os arquivos .ts dentro da pasta src
  ],
  "exclude": [
    "node_modules",                      // Exclui a pasta node_modules
    "dist",                              // Exclui a pasta dist
    "test"                               // Exclui a pasta de testes, caso haja
  ]
}
